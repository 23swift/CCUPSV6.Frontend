import React from 'react'
import { Box } from '@material-ui/core'

const CCUPSPageContent = (props) => {
    return (
       
        <Box mr={2} ml={2}>
         {props.children}
      </Box>
    )
}

export default CCUPSPageContent
